server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/clinicdb
    username: clinic
    password: clinic123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
logging:
  level:
    org.hibernate.SQL: debug

app:
  jwt:
    secret: "CHANGE_ME_TO_A_LONG_RANDOM_STRING_AT_LEAST_64_CHARS________________________"
    access-token-exp-min: 60
